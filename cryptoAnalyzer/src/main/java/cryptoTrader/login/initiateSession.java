package cryptoTrader.login;

/**
 * Initiate Session interface used to:
 * - create a session to run the mainUI once the user's credentials are valid.
 *
 * @author Hala Elewa
 */
public interface initiateSession {
	public void startSession();
}
